{
	"name": "youngmonkey-sls",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"invoke-local": "npx sls invoke local --path local-invoke.json --function notify",
		"invoke": "npx sls invoke --path local-invoke.json --function notify",
		"test": "jest"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"eslint": "^8.25.0",
		"jest": "^29.1.2",
		"serverless": "^3.22.0"
	},
	"dependencies": {
		"node-fetch": "^3.2.10"
	}
}
